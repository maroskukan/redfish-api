# Recipes

## Environment

```bash
# Create new virtual environment
mkvirtualenv redfish-api

# If not activated automatically, activate the environment
workon redfish-api

# Install dependencies
pip install -r requirements.txt

# Once you finished, deactivate the environment
deactivate
```
